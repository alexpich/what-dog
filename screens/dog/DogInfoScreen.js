import React from "react";
import DogInfo from "../../components/DogInfo";

const DogInfoScreen = () => {
  return <DogInfo />;
};

DogInfoScreen.navigationOptions = {
  headerTitle: "DogInfo",
};

export default DogInfoScreen;
